# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Office / temp lock files
~$*
*.tmp

# R
.Rhistory
.RData
.Rproj.user/
.Ruserdata

# Editor cruft
*.swp
*~

# Local dev tooling (preview server config) — not course content
.claude/
_tools/__pycache__/

# Instructor answer keys — never publish. Kept locally; release via Canvas
# after grading. (Practice-lab answer_key.html stays public by design.)
*_KEY.html

# Instructor-only pre-publication review — contains answer-key defects and
# grading gaps. Never publish. (RECOMMENDATIONS.* is tracked and currently DOES
# publish despite saying otherwise — see PREPUB_REVIEW.md B19.)
# NB: something in this repo auto-renders .md -> .html (pandoc). Ignore BOTH forms --
# ignoring only the .md is how RECOMMENDATIONS.html ended up published.
PREPUB_REVIEW.*
